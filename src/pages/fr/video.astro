---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Section from "../../components/Section.astro";
import VideoGallery from "../../components/VideoGallery.astro";
import FinalCTA from "../../components/FinalCTA.astro";
import { getLang, getT } from "../../i18n/utils";
const lang = getLang(Astro);
const t = getT(lang);
---
<BaseLayout title={t.nav.video}>
  <Section noBorder>
    <header class="video-intro">
      <p class="eyebrow">{t.video.eyebrow}</p>
      <h1 class="video-intro__title">
        {t.video.title.map((line, i) => <>{i > 0 && <br />}{line}</>)}
      </h1>
      <p class="video-intro__lead">{t.video.lead}</p>
    </header>
  </Section>

  <Section noBorder size="sm">
    <VideoGallery />
  </Section>

  <Section size="lg">
    <FinalCTA />
  </Section>
</BaseLayout>

<style>
  .video-intro { max-width: 760px; padding: 24px 0 8px; }
  .eyebrow {
    font-size: 11px;
    letter-spacing: var(--ls-extra);
    text-transform: uppercase;
    color: var(--color-text-mute);
    margin: 0 0 24px;
  }
  .video-intro__title {
    font-family: var(--font-display);
    font-size: var(--fs-display);
    line-height: 0.9;
    letter-spacing: 0.005em;
    margin: 0 0 24px;
    text-transform: uppercase;
  }
  .video-intro__lead { margin: 0; font-size: 16px; color: var(--color-text-dim); line-height: 1.7; max-width: 56ch; }
  @media (max-width: 760px) {
    .video-intro { padding: 8px 0 4px; }
    .video-intro .eyebrow { font-size: 13px; letter-spacing: 0.28em; }
    .video-intro__title { font-size: clamp(56px, 14vw, 84px); }
    .video-intro__lead { font-size: 17px; line-height: 1.7; }
  }
</style>
